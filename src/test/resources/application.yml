spring:
  datasource:
    url: jdbc:h2:person;MODE=POSTGRESQL;INIT=CREATE SCHEMA IF NOT EXISTS ciap_pf
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    generate-ddl: false
  main:
    banner-mode: "off"
